# Markowitz India Dashboard - Version 1

Run:

```bash
pip install -r requirements.txt
streamlit run app.py
```

This first version includes simulated demo data, CSV upload, data cleaning, annualized return and volatility, normalized price charts, a correlation heatmap, and cleaned-data download.

Demo data is simulated and must not be treated as real market data.
